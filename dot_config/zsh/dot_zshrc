zfiles=(
    ".zbase"
    ".zenv"
    ".zcomp"
    ".zhistory"
    ".zfunctions"
    ".zalias"
    ".zhook"
    ".zactivate"
    ".zthema"
)

for zfile in $zfiles
do
    [ -f $ZDOTDIR/$zfile ] && source $ZDOTDIR/$zfile
done
